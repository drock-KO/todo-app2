<h1>Projects#show</h1>
<p><%= @project.project %>のタスク一覧</p>

<% unless @tasks.empty? %>
  <ul>
    <% @tasks.each do |task| %>
      <%= link_to "#{task.task}", project_task_path(task.project, task) %>
    <% end %>
  </ul>
<% end %>

<%= link_to "タスクを追加する", new_project_task_path(@project) %>


